// for siyah

#include <ansi.h>
#include <mudlib.h>
inherit ARMOUR;

reset(arg) {
    if(arg) return;
    set_name("cap");
    set_alt_name("hat");
    set_short(B_BLUE_F"ruinic pointed cap"NORM);
    set_long(B_BLUE_F"A hat much like those worn by wizards.\n"+NORM);
    set_type("helm");
    set_ac(11);
    set_bonus_int(3);
    set_weight(10);
    set_value(850);
    set_ego(70);
}
