


int top(){
  object *users;
  int top,tmp,i;
  users = filter_array(users());
  write("Top:\n");
    for(i = 0; i < sizeof(users) i++)
    { 
      tmp = "cmds/std/_stats.c"->total_score(users[i]);
      write("\n"+tmp);
    }
 return 1;

}
