#include <ansi.h>
#include <bar.h>

string *query_cmd_aliases() { return
({"exp", }); }


status main(){ 
    write(PRINTBAR+"\n");
     "/cmds/wiz/_levexp"->main();
    write(PRINTBAR+"\n");
      return 1;
}

status help(){ return main(); }
